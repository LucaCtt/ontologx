# Experiment configuration
EXPERIMENT_NAME = "ait_llama32_ex"
CORRECTION_STEPS = 3

# Models configuration
LLM_NAME = "us.meta.llama3-3-70b-instruct-v1:0" # Bedrock
# LLM_NAME = "Qwen/Qwen3-Coder-32B-A3B-Instruct-FP8" # VLLM/HF
EMBEDDINGS_NAME = "Alibaba-NLP/gte-multilingual-base"

# Backend configuration
VLLM_ADDITIONAL_ARGS = "--tensor-parallel-size 4 --enable-expert-parallel --max-model-len 32768 --gpu-memory-utilization 0.90 --quantization fp8 --kv-cache-dtype fp8 --tool-call-parser hermes"

# Access tokens
HF_TOKEN =
AWS_ACCESS_KEY_ID =
AWS_SECRET_ACCESS_KEY =
AWS_ROLE_ARN =
OPENAI_API_KEY =
